<nav class="navbar navbar-inverse" style="margin-bottom: 5px">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('app_homepage') }}">Wake Up Cafe</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li {% if app.request.attributes.get('_route') == 'app_homepage' %}class="active"{% endif %}><a href="{{ path('app_homepage') }}">Home</a></li>
                <li {% if app.request.attributes.get('_route') == 'app_reservation' %}class="active"{% endif %}><a href="{{ path('app_reservation') }}">Reservation</a></li>
                <li {% if app.request.attributes.get('_route') starts with 'app_blog' %}class="active"{% endif %}><a href="{{ path('app_blog_list') }}">Blog</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {% if app.user and app.user.hasRole('ROLE_USER') %}
                    <li {% if app.request.attributes.get('_route') == 'app_view_reservations' %}class="active"{% endif %}><a href="{{ path('app_view_reservations') }}">Moje Rezerwacje</a></li>
                {% endif %}

                {% if app.user and app.user.hasRole('ROLE_SUPER_ADMIN') %}
                    <li><a href="{{ path('app_admin_homepage') }}">Admin Panel</a></li>
                {% endif %}

                {% if app.user %}
                    <li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
                {% endif %}

                {% if not app.user %}
                    <li><a href="{{ path('fos_user_security_login') }}">Login</a></li>
                    <li><a href="{{ path('fos_user_registration_register') }}">Register</a></li>
                {% endif %}

            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>