{% extends '@App/layout.html.twig' %}

{% block body %}
    <div class="container">

        <h1>{{ post.title }}</h1>

        <div>
            {{ post.content|raw }}
        </div>

        <div class="text-right">
            <a class="btn btn-default" href="{{ path('app_blog_list') }}"> <i class="fa fa-arrow-circle-left " aria-hidden="true"></i> Back</a>
        </div>

    </div>
{% endblock %}